
@{
    ViewData["Title"] = "Films";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Филми</h1>

@*<iframe width="420" height="345" src="https://www.youtube.com/embed/1akixU65dDY"> </iframe>
<iframe width="420" height="345" src="https://www.youtube.com/embed/YGEZ_936HEY" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
<iframe width="420" height="345" src="https://www.youtube.com/embed/Mq4m3yAoW8E" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>*@


    
    @if (User.IsInRole("User") && User.Identity.IsAuthenticated || User.Identity.IsAuthenticated)
    {
    <hr class="hr-2 bg-primary">
    <div id="products" class="mt-3">
        @for (int i = 0; i < Model.Products.Count; i += 5)
        {
            <div class="products-row row d-flex justify-content-around my-5">
                @for (int j = i; j < i + 5; j++)
                {
                    if (j < Model.Products.Count)
                    {
                        <div class="product col-md-2">

                            <h5 class="text-center mt-3">@Model.Products[j].Id</h5>
                            <h5 class="text-center mt-3">@Model.Products[j].Title</h5>
                            <h5 class="text-center mt-3">@Model.Products[j].Author</h5>
                            <h5 class="text-center mt-3">@Model.Products[j].Description</h5>
                            <h5 class="text-center mt-3">@Model.Products[j].ISBN</h5>
                            <h5 class="text-center mt-3">@Model.Products[j].Publishing</h5>
                            <h5 class="text-center mt-3">@Model.Products[j].YearOfPublishing</h5>
                            <h5 class="text-center mt-3">Price: $@Model.Products[j].Price.ToString("F2")</h5>
                            <div class="image-holder d-flex justify-content-center">
                          
                                <img class="img-thumbnail product-image" src="@Model.Products[j].Picture" alt="...">
                          
                            </div>


                            <div class="card-footer d-flex justify-content-around text-center bg-white pl-0 pr-0 pt-0 pb-0">
                                @{
                                    string url = Url.Action("Create", "Orders", new { id = @Model.Products[j].Id });
                                }

                                <button type="button" class="btn btn-primary btn-md" onclick="location.href='@url'">
                                    Купи
                                </button>



                            </div>


                        </div>

                    }
                    else
                    {
                        <div class="product col-md-2 empty">
                        </div>
                    }

                }
            </div>
        }
    </div>
    }
    



